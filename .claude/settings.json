{
  "permissions": {
    "allow": [
      "Bash(*)",
      "Bash(python -c \"from ui.main_window import MainWindow, ProjectTreePanel, ToolBar, ToolInterface; print\\(''All imports OK''\\)\")"
    ]
  }
}
